import React from 'react';
import './Card.scss';
import CloseButton from '../buttons/closeButton';

const Card = () => {
    return (
        <div className='card'>
            Card
            <CloseButton />
        </div>
    );
};

export default Card;